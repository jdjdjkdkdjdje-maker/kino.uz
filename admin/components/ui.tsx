import{AlertTriangle,RefreshCw}from'lucide-react';
export function PageHeader({title,description,action}:{title:string;description:string;action?:React.ReactNode}){return <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="page-title">{title}</h1><p className="mt-1 text-sm text-zinc-500">{description}</p></div>{action}</div>}
export function Loading(){return <div className="grid min-h-64 place-items-center"><div className="h-9 w-9 animate-spin rounded-full border-2 border-brand-500 border-t-transparent"/></div>}
export function ErrorState({message,retry}:{message:string;retry:()=>void}){return <div className="card grid min-h-64 place-items-center p-8 text-center"><div><AlertTriangle className="mx-auto mb-3 text-amber-400"/><p className="text-zinc-300">{message}</p><button className="btn-secondary mt-4" onClick={retry}><RefreshCw size={16}/> Qayta urinib ko‘rish</button></div></div>}
export const fmt=(n:number|string|bigint=0)=>new Intl.NumberFormat('uz-UZ').format(Number(n));
export function Status({value}:{value:string|boolean}){const active=value===true||value==='ACTIVE';const draft=value==='DRAFT';return <span className={`badge ${active?'bg-emerald-500/10 text-emerald-400':draft?'bg-amber-500/10 text-amber-400':'bg-zinc-500/10 text-zinc-400'}`}>{typeof value==='boolean'?(value?'Faol':'O‘chirilgan'):active?'Faol':draft?'Qoralama':'Faol emas'}</span>}
