import { Body, Controller, Delete, Get, Post, Query, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { AuthUser, CurrentUser } from '../common/decorators/current-user.decorator'; import { JwtAuthGuard } from '../common/guards/jwt-auth.guard'; import { HistoryQueryDto, SaveHistoryDto } from './history.dto'; import { HistoryService } from './history.service';
@ApiTags('history') @Controller('history') @UseGuards(JwtAuthGuard) @ApiBearerAuth() export class HistoryController{constructor(private readonly s:HistoryService){} @Get()all(@CurrentUser()u:AuthUser,@Query()q:HistoryQueryDto){return this.s.all(u.id,q)} @Post()save(@CurrentUser()u:AuthUser,@Body()d:SaveHistoryDto){return this.s.save(u.id,d)} @Delete()clear(@CurrentUser()u:AuthUser){return this.s.clear(u.id)}}
