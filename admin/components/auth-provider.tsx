'use client';
import{api,tokenStore}from'@/lib/api';import{usePathname,useRouter}from'next/navigation';import{createContext,useContext,useEffect,useState}from'react';
type User={id:string;name:string;email?:string;role:'ADMIN'|'USER';avatarUrl?:string};type AuthValue={user:User|null;ready:boolean;login:(identifier:string,password:string)=>Promise<void>;logout:()=>Promise<void>};
const AuthContext=createContext<AuthValue|null>(null);
export function AuthProvider({children}:{children:React.ReactNode}){const[user,setUser]=useState<User|null>(null),[ready,setReady]=useState(false);const router=useRouter(),pathname=usePathname();useEffect(()=>{(async()=>{if(tokenStore.access){try{const u=await api<User>('/users/me');if(u.role==='ADMIN')setUser(u);else tokenStore.clear()}catch{tokenStore.clear()}}setReady(true)})()},[]);useEffect(()=>{if(ready&&!user&&pathname!=='/login')router.replace('/login');if(ready&&user&&pathname==='/login')router.replace('/')},[ready,user,pathname,router]);async function login(identifier:string,password:string){const d=await api<any>('/auth/admin/login',{method:'POST',body:JSON.stringify({identifier,password})});tokenStore.set(d.accessToken,d.refreshToken);localStorage.setItem('kinotv_user',JSON.stringify(d.user));setUser(d.user);router.replace('/')}async function logout(){try{await api('/auth/logout',{method:'POST',body:JSON.stringify({refreshToken:tokenStore.refresh})})}catch{}tokenStore.clear();setUser(null);router.replace('/login')}return <AuthContext.Provider value={{user,ready,login,logout}}>{children}</AuthContext.Provider>}
export function useAuth(){const x=useContext(AuthContext);if(!x)throw new Error('AuthProvider topilmadi');return x}
