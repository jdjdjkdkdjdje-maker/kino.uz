'use client';import{useState}from'react';import{useAuth}from'@/components/auth-provider';import{Clapperboard,Eye,EyeOff,Lock,Mail}from'lucide-react';
export default function LoginPage(){const{login}=useAuth();const[id,setId]=useState('admin@kinotv.uz'),[password,setPassword]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{await login(id,password)}catch(e){setError(e instanceof Error?e.message:'Kirishda xato')}finally{setLoading(false)}}return <main className="relative grid min-h-screen place-items-center overflow-hidden p-5"><div className="absolute left-1/2 top-0 h-96 w-96 -translate-x-1/2 rounded-full bg-brand-600/20 blur-[120px]"/><form onSubmit={submit} className="card relative w-full max-w-md p-7 sm:p-9"><div className="mb-8 text-center"><span className="mx-auto mb-4 grid h-14 w-14 place-items-center rounded-2xl bg-gradient-to-br from-brand-500 to-fuchsia-600 shadow-glow"><Clapperboard/></span><h1 className="text-2xl font-bold">KinoTV boshqaruvi</h1><p className="mt-2 text-sm text-zinc-500">Davom etish uchun admin hisobiga kiring</p></div>{error&&<div className="mb-5 rounded-xl border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-400">{error}</div>}<label className="label">Email yoki telefon</label><div className="relative mb-4"><Mail className="absolute left-3 top-3 text-zinc-600" size={18}/><input className="input pl-10" value={id} onChange={e=>setId(e.target.value)} required/></div><label className="label">Parol</label><div className="relative mb-6"><Lock className="absolute left-3 top-3 text-zinc-600" size={18}/><input className="input px-10" type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Kamida 8 belgi" required minLength={8}/><button type="button" className="absolute right-3 top-3 text-zinc-500" onClick={()=>setShow(!show)}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div><button className="btn-primary w-full" disabled={loading}>{loading?'Kirilmoqda…':'Kirish'}</button><p className="mt-5 text-center text-xs text-zinc-600">Xavfsiz JWT avtorizatsiyasi</p></form></main>}
