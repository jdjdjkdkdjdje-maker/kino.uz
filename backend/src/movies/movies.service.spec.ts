import{BadRequestException}from'@nestjs/common';import{MoviesService}from'./movies.service';
describe('MoviesService video litsenziyasi',()=>{const base={title:'Film',description:'Yetarlicha uzun film tavsifi',posterUrl:'https://example.com/p.jpg',bannerUrl:'https://example.com/b.jpg',releaseYear:2026,durationMinutes:90,rating:8,language:'uz',country:'UZ',categoryIds:[],genreIds:[]};const cache:any={deleteByPrefix:jest.fn()};it('litsenziya tasdig‘isiz katalog filmi videosiz yaratiladi',async()=>{const prisma:any={movie:{create:jest.fn().mockResolvedValue({id:'m1',...base,videoUrl:null,isLicensedVideo:false})}};const s=new MoviesService(prisma,cache);const movie=await s.create(base as any);expect(movie.videoUrl).toBeNull()});it('video URLsiz litsenziya belgisini rad etadi',async()=>{const s=new MoviesService({}as any,cache);await expect(s.create({...base,isLicensedVideo:true}as any)).rejects.toBeInstanceOf(BadRequestException)});it('public detail litsenziyasiz URLni yashiradi',async()=>{const row={id:'00000000-0000-4000-8000-000000000001',slug:'film',status:'ACTIVE',videoUrl:'https://example.com/private.mp4',subtitleUrl:null,isLicensedVideo:false,genres:[],categories:[],actors:[],directors:[]};const prisma:any={movie:{findFirst:jest.fn().mockResolvedValue(row),findMany:jest.fn().mockResolvedValue([])}};const s=new MoviesService(prisma,cache);const result:any=await s.findOne(row.id);expect(result.videoUrl).toBeNull();expect(result.hasFullVideo).toBe(false)});});
